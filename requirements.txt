# Python 3.10+
#
# Pinned to what this was built and tested against. The OCR stack
# (rapidocr-onnxruntime) is not here on purpose: it is installed inside the
# Docker sandbox image, never on the host, so the OCR container can keep
# running with networking disabled.
#
#     pip install -r requirements.txt


# the agent loop
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
langchain-core==1.6.1

# model providers — any subset works; local Ollama needs no key
langchain-ollama==1.1.0
langchain-groq==1.1.3
langchain-openai==1.6.0
langchain-google-genai==4.3.7
google-genai==2.21.0
ollama==0.6.2
groq==0.37.1

# the web app
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
pydantic==2.13.5

# documents in
pypdf==6.16.2
pymupdf==1.28.2
openpyxl==3.1.5
pillow==12.3.0

# documents out
python-docx==1.2.0
python-pptx==1.0.2

# retrieval and memory
chromadb==1.5.9

# the live browser — drives the Chrome that Steel launches, over CDP.
# `playwright install` is deliberately not needed: this attaches to Steel's
# browser rather than downloading one of its own.
playwright==1.56.0

# the sandbox, and reaching the network
docker==7.2.0
httpx==0.28.1
requests==2.34.2
python-dotenv==1.2.3
